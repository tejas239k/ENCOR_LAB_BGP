# ENCOR_LAB_BGP
Enterprise Level lab for BGP: Border Gateway Protocol is an external gateway protocol used in between the organizations and ISP's. The eBGP is used to connect two different ASN's also the iBGP is used within the AS to propagates the underlying routes to other BGP neighbors. In this lab we've also redistributed routes just to see the origin symbol ?
